#include <stdio.h>
#include <stdlib.h>


int ft_strcmp(char *s1, char *s2)
{
    char *first;
    char *second;
    
    if(first == 0)
        return -1;

    else if(second == 0)
        return 1;
    else
        return 0;
}